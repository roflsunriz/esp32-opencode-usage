# 変更履歴

このプロジェクトの変更履歴はKeep a Changelog形式で記録します。

## [Unreleased]

## [0.4.1] - 2026-09-26

### Fixed

- CI と Dependabot の分類の実行順が前後しても更新を取りこぼさないよう、同じ PR 番号と head SHA を再照合する経路を追加した。
- 取得失敗の原因をLCDとUSB記録で区別できるように、応答超過と応答不正の状態表示を分離し（`OpenCode response too large`／`OpenCode response invalid`）、応答不正・超過時に内容・認証・ヘッダー値を含まない診断（HTTP状態・宣言長・受信バイト・内容種別）をUSBへ送出するようにした。宣言長より短い本体の到着は通信の途中切断として扱い、`OpenCode HTTPS failed` として報告するようにした。

### Changed

- 依存更新を安全に省力化するため、Dependabot の patch／minor PR を既存 CI の全チェック成功後に自動取り込みし、失敗ジョブを一度再実行する設定を追加した。

## [0.4.0] - 2026-09-23

### Added

- 状態表示で次の取得までの残り時間が分かるように、Usageタブのフッターへ更新状態と残り秒数（例: `OpenCode updated - next 42s`）の表示を追加した。取得間隔が不明な間は従来どおり状態のみを表示する。
- 自動消灯の調整を固定9択からスライダー方式へ変えるため、Displayタブへ0〜59分のスライダー、0〜24時間のスライダー、60〜600秒（60秒刻み）の取得間隔スライダーとスクロールバーを追加した。0分0時間は自動消灯オフ（常時点灯）と同じになる。従来の9択の保存値は引き続き有効で、PC操作画面の9択とも互換性を保つ。
- USBの `display` 操作へ任意の `pollIntervalSec`（60〜600の60秒刻み）を追加し、Wi-Fi・認証を保ったまま取得間隔だけを変更できるようにした。`display` のACKへ `pollIntervalSec` を含めた。
- ペンを押し付けたままなぞるドラッグ操作に対応し、スライダーの値調整とDisplayタブのスクロール（スライダー行・スクロールバー・内容ドラッグ）を接触継続中も追従するようにした。READMEへ実機画面の例を追加した。

### Fixed

- 0分0時間の指定を期限0msと扱って即消灯していた問題を修正し、常時点灯し続けるようにした。
- Displayタブ表示中はフッターの残り秒数が更新されない問題を修正し、どちらのタブでも毎秒更新するようにした。
- PC接続時のDTRパルスがESP32の再起動と画面反転の保存付き切替を起こしていた問題を修正し、接続時にDTR/RTSを落としてping応答で互換確認するようにした。
- スクロール内容がタブヘッダーへはみ出して描画されていた問題と、タブ上で始まった接触がスクロールになっていた問題を修正し、タブをスクロール対象から外した。

## [0.3.0] - 2026-09-22

### Changed

- 公式コンソールの移行（`/console` 配下・使用量API）に追従し、PCとESP32の取得処理を `GET /console/api/go/status`（`x-org-id` ヘッダー・microCents単位の `meters`）へ切り替えた。旧来の `/_server` 取得・Seroval解析・取得先ID検出を撤去し、USB設定の `queryId` は互換保持の空欄にした。
- 認証をコンソールのセッションCookie（`__Host-console_session`）へ切り替えた。旧形式の認証レコードは本体起動時に破棄するため、更新後はPC操作画面からの再ログインと再保存が1回必要になる。

### Fixed

- 権限破損で読み取れなくなった `.platformio` の `tool-esptoolpy` を隔離し、再取得でESP32ビルドを復旧した。隔離物は `.private/` に保管している。

## [0.2.2] - 2026-09-16

### Added

- 基板ごとのタッチ配線差に対応するため、BOOT長押しの校正を3点（左上・右上・左下）にし、同一行・同一列の測定差から軸方向を自動判定して保存するよう追加した。旧2点記録は入替として移行する。校正状態と押圧閾値を `ping` 応答で確認できるよう追加した。

### Fixed

- 非入替配線の基板で校正後も位置が大きくずれる問題を修正。2点対角では入替と非入替を区別できないため軸判定を3点化した。校正の押下は普段の強さで行うよう画面表示と手順を改めた。
- 無接触のPENIRQノイズがバックライト期限を更新し続けて自動消灯を妨げていた問題を修正。指なしのグリッチは無音で捨て、読み取り成功後のみバックライトを更新する。
- 開発用フォーマッタの挙動を最新に保つため、ruffを0.16.7へ更新した。
- ZIP展開の脆弱性修正とWindows単体テストの修正を取り込むため、PlatformIO Coreを6.2.0へ更新した。

## [0.2.1] - 2026-09-14

### Fixed

- タブ切替後に画面右側の前ページが残る可能性を修正。8-bit Spriteの全幅320ピクセルを消去してから差分を転送する。

## [0.2.0] - 2026-09-14

### Added

- タッチペンの軽い押下と基板ごとの位置ずれに対応するため、BOOT長押しからの2点位置・押圧感度調整とNVS保存を追加した。調整失敗時は以前の値を保持する。

### Changed

- 通常画面とタブ入力を通知用CYDと同じTFT_eSPI描画、別SPIのXPT2046入力へ揃えた。既存の実機確認済み軸入れ替えと消灯中の復帰操作を保ち、配布ソースにも新しい入力ドライバーを含める。
- 画面の定期更新とタブ操作で全画面を繰り返し転送しないよう、8-bit Spriteの16行帯を比較し、変化した帯だけをまとめて転送する。回転と校正後は全画面を更新する。

## [0.1.1] - 2026-09-10

### Added

- 設置する向きに合わせられるよう、BOOTボタンのクリックで画面を180度回転し、再起動後も向きを保持する機能を追加した。反転後のタッチ操作、消灯中の点灯、長押しの重複防止、通信中のクリック受付に対応する。
- Wi-Fi・認証・消灯時間を保持して画面の向きの保存形式へ移行し、PCからの設定変更でも向きを維持するようにした。

### Fixed

- タブ切り替えや設定操作で画面がちらつく問題を修正し、描画途中の消去画面を見せずに表示を更新するようにした。
- 更新時の設定消失を避けるため、配布物の書き込み案内に設定を保つ更新と初回導入の違いを明記した。

## [0.1.0] - 2026-09-09

### Added

- LCDのDisplayタブで9段階の自動消灯時間を選べるようにし、タッチでの再点灯と再起動後の設定保持を追加した。消灯中もWi-Fi更新を継続する。
- 初期設定画面からも消灯時間だけを変更できるよう、認証再入力不要の保存と手動点灯を追加した。

- OpenCode Goの5時間・週間・月間制限を手元で確認できるよう、ESP32-2432S028R向けの使用率・ドル換算額・プログレスバー表示を追加した。
- 公式サイトのデプロイ変更に追従できるよう、取得関数の動的検出と実レスポンスの厳格な数値変換を追加した。
- 再起動後もログイン状態を再利用できるよう、公式認証画面を使うログイン導線とWindows DPAPIによる認証保存を追加した。
- 初回のログインと設定を行える日本語／英語の操作画面と、USB経由でESP32へ認証を保存する補助プログラムを追加した。
- PCを常時稼働させず利用できるよう、ESP32がNTP同期後に証明書を検証してOpenCodeへ直接HTTPS接続する方式を追加した。
- 元の機器状態を戻せるよう、全フラッシュのバックアップ・照合・復元スクリプトを追加した。
- 誤った成功表示やデータ破損を防ぐため、入力境界テスト、設定の一括保存、相関ID付き保存ACK、古いデータの表示を追加した。
- 再現可能な運用のため、ビルド・テスト・リリースのワークフロー、使用開始・更新・復旧・検証文書を追加した。

### Fixed

- Windows CIの改行差による整形失敗と標準PowerShellの文字化けを防ぐため、改行規約とUTF-8 BOMを明示した。

- 実機検証で見つかった空文字設定の保存失敗を修正し、Wi-Fi設定の削除を完了できるようにした。
- USB接続が停止してポートを占有し続ける問題を避けるため、USBシリアル処理をPython/pySerialへ分離し、接続・終了の時間制限を設けた。
- LCD再起動時の復帰と認証状態の整合を保つため、起動通知と無効化された表示キャッシュの破棄を追加した。

- 証明書の転記不備によるHTTPS失敗を防ぐため、公式CAファイルからの生成・照合手順を追加した。
- 別の機器へ秘密情報を送信しないよう、認証保存前に対応ファームウェアの応答を確認するようにした。

### Security

- 画像保存用の開発依存Pillowを12.3.0へ更新し、GitHubが報告した画像処理の脆弱性に対応した。
