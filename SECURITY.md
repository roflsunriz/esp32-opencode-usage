# セキュリティポリシー

このプロジェクトは、Windows PCで初回ログインとUSB設定を行い、認証CookieをESP32へ保存して、以後ESP32が公式HTTPSサービスへ直接接続する構成です。Cookie、Wi-Fiパスワード、ワークスペース、取得先ID、シリアル通信内容の問題は公開issueへ書かないでください。

## 脆弱性の報告

非公開の報告フォームが利用可能な場合は、GitHubの [Private vulnerability reporting](https://github.com/roflsunriz/esp32-opencode-usage/security/advisories/new) を利用してください。利用できない場合は、公開issueで非公開の連絡窓口の提供だけを依頼し、秘密情報や再現手順を投稿しないでください。

報告には、影響を受けるコミットまたはバージョン、再現手順、影響範囲、再現に必要な最小限の入力、連絡可能な窓口を含めてください。実際のCookie、パスワード、個人情報、完全なログは送らず、無効化または置換した値を使います。秘密情報が誤って公開された場合は、まず該当する認証を失効・再発行し、その後に報告してください。

報告を受けた後は、再現性と影響を確認し、修正、公開範囲、クレジットの扱いを報告者と調整します。対応時期を保証するSLAは設けていませんが、確認できた内容には可能な範囲で返信します。

## 保存される情報

- PC側のOpenCodeセッションはWindows DPAPIで保護します。
- 初期設定後、ESP32のNVSにWi-Fiパスワード、認証Cookie、ワークスペース、取得先IDが残ります。
- 通常のESP32フラッシュは暗号化されていません。機器と全フラッシュバックアップは秘密情報として扱い、第三者へ渡さないでください。
- `.private/` はPC側の認証情報とフラッシュバックアップを含むため、同期、画面共有、公開リポジトリへ含めないでください。
- PCのlogoutはPC側のセッションだけを削除します。ESP32の保存情報を削除する場合は、USB接続した初期設定画面から削除操作を行います。

## 直接取得経路の注意

2026-09-09に、正しいGTS Root R4＋GlobalSign Root CAの信頼束を使ったHTTPS取得、NTP同期、再起動後の認証保持、PC設定アプリ停止中の継続更新を実機確認しました。

## 利用者側の基本対策

- 初回設定画面の完了表示と、LCDの直接更新確認を見届けてからPCを終了する。
- ESP32を譲渡・廃棄する前に、初期設定画面からESP32の設定と認証を削除する。
- 全フラッシュのバックアップを共有する前に、CookieとWi-Fiパスワードが含まれることを確認する。
- 外部ページの形式変更やHTTPSエラーを、検証を省略して成功扱いにしない。
