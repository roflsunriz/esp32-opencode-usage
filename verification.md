# 検証

## response invalidの間欠表示の調査と修正（2026-09-26）

所有者の報告はLCD状態行の `OpenCode response invalid` が時々出ること。文字列の出どころは `firmware/src/network_client.cpp` の取得失敗表示だけであり、PC側に同文言はない。

- `bun start usage` は正常（5時間・週間・月間の3期間を取得）。
- 実応答は796バイト・`Content-Type: application/json`・`Content-Length: 796`（ESP32相当のidentity要求時。Bun既定は `br`＋chunkedでCloudflareが付与する）。200/200の連続取得と、不正Cookie→401・不正ワークスペース→404の対応を確認した。
- 実応答と同形（識別子のみ置換）のnativeパースは成功し、現在の形式では本体・PCとも通る（`firmware/test/test_response_status`）。
- COM7実機は製品の `DeviceConnection` で1秒以内にping互換（`firmware=opencode-go-lcd`・`setupSchema=3`）を確認し、直接取得の成功（`renderCount` 1→2・更新時刻つき3期間）と `OpenCode updated` をUSBで確認した。15分の監視では再発しなかった。
- よって取得時点の一時的な応答異常（空本体・未知形式・超過・宣言長未満の途中切断）が有力だが、旧実装には原因特定を不能にする3欠陥があった。超過と不正が同一文言、宣言長不一致の途中切断を不正として報告、不正時に診断を出さない。
- 修正は状態文の分離（超過は `OpenCode response too large`）、宣言長不一致の途中切断を `OpenCode HTTPS failed`（通信診断付き）へ変更、応答不正・超過時に内容・認証・ヘッダー値を含まない診断（`component":"opencode_response`、HTTP状態・宣言長・受信バイト・内容種別）をUSB送出とした。nativeは5件追加で全46件、ホスト37件、ESP32ビルドが成功した。
- 新状態文の実機表示は失敗注入ができないため未検証で、次回発生時のUSB診断で確認する。再試行の自動化は頻度が分かってから判断する。

## v0.4.0リリース（2026-09-23）

`package.json` を0.4.0に上げ、`Unreleased` を `## [0.4.0] - 2026-09-23` に移した。mainのCI成功後に `v0.4.0` タグを公開し、Releaseワークフローが成功して公開ノートがCHANGELOGの0.4.0節と一致することを確認した。4成果物を展開し、ホストZIPの `start.cmd`・USB worker（DTR修正入り）・ファームウェアZIPの個別イメージと `firmware-full.bin`（bootloaderとアプリ領域のESP32 magic確認）・秘密情報やキャッシュを含まないことを確認した。

## Displayタブのスライダー化と取得残り時間表示（2026-09-23、実機検証済み）

作戦書 `esp32-ui-style.md` に基づき、Displayタブを固定9択から0〜59分・0〜24時間・取得間隔60〜600秒（60秒刻み）のスライダー＋スクロールバーへ変え、Usageタブのフッターへ次の取得までの残り秒数表示を追加した。0分0時間は自動消灯オフ（常時点灯）と同じになる。従来の9択保存値は有効なまま移行する。

- native 39件・ホスト37件・ESP32ビルド・`bun audit`（脆弱性なし）・mypy strict・ruff整形・CA照合が成功した。
- COM7基板（ESP32-D0WD-V3・4MB）へ全flash退避（4,194,304バイト、SHA-256 `83F5D058…3AC4E`、`.private/backups/before-slider-ui-20260923.bin`）と `verify-flash` のdigest一致を確認してから試験した。自動リセット接続は `Wrong boot mode detected (0x13)` のため、BOOT保持中に読み出し・書き込みを行い、読み出し後は `--after no_reset` で待機維持して `--before no_reset` で照合した。
- アプリ領域のみ更新し、NVSの認証・Wi-Fi・校正値を保持した。起動後にESP32の直接取得（`hasUsage=true`）を確認した。
- 実機LCDのUSB読出しでUsageタブの3期間・金額・バー・リセット時間と `Usage updated - next 31s`、Displayタブの3スライダー・スクロールバー・`Off after: 2h 10m`・`Update 120s` を画像確認した（`docs/lcd-display-sliders.png`）。
- ペンのドラッグ操作に対応し、押し付けたままなぞると分・時間・取得間隔が追従すること（2h10m・120sへの変更）を所有者が実機で確認した。
- 0分0時間の指定で即消灯する問題を修正し、常時点灯し続けることを所有者が実機で確認した。
- Displayタブ表示中はフッターの残り秒数が更新されない問題を修正した。Usageタブの実時間カウントダウンは所有者が実機で確認済みである。
- 内容ドラッグでのスクロール（スライダー行以外をなぞると全体がスクロールする方式）を追加し、所有者が実機で確認した。nativeは41件に増えた。
- タブヘッダーへの内容のはみ出し描画と、タブ上開始の接触によるスクロールを修正し、タブがスクロール対象外であることを所有者が実機で確認した。
- PC接続時のDTRパルスが再起動と画面反転の保存付き切替を起こしていた問題を修正し、修正版workerでは接続時の再起動がなくping互換を確認した。旧版では接続でuptimeが約500msへ戻ることを対比確認した。
- 試験終了時は元の全4MBを書き戻し、`verify-flash` の完全一致を確認した。復元後は退避時の設定値（取得間隔60秒・消灯7200秒）へ戻り、直接取得の再開を確認した。
- COM7の読取のみの接続確認では `firmware=opencode-go-lcd`・`setupSchema=3` の応答を確認した。

## コンソール移行への追従（2026-09-22、実機検証済み）

公式Goコンソールが `/console` 配下へ移行し、`/_server`・Seroval・`auth` Cookieが廃止された。`GET /console/api/go/status`（`x-org-id` ヘッダー・`__Host-console_session`）の `access.meters`（`fiveHour`/`week`/`month`、`limitMicroCents`/`usedMicroCents`）へPCとESP32を移行した。金額は1セント=1,000,000 microCents（1ドル=100,000,000）で換算し、使用率は `used/limit*100`、リセットは `resetsAt`（月間は `access.endsAt`）から求める。

- ホスト41件・native 36件・ESP32ビルド・CA照合・`bun audit`（脆弱性なし）・mypy strict・ruff整形が成功した。
- 実セッションでPCの取得を実測し、月間66〜67%が公式表示と一致することを確認した。
- COM7基板（ESP32-D0WD-V3・4MB）へNVS保持のupload方式で書き込み、旧 `auth=` 記録の破棄と初期設定画面への復帰を確認した。PC操作画面から再ログイン・再保存し、ESP32の直接更新（`lastDirectUpdate`）を確認した。
- 実機LCDのUSB読出しで5H `$0.47 / $12.00`・WEEK `$0.47 / $30.00`・MONTH `$40.24 / $60.00` と `OpenCode updated` を画像確認した（`.private/lcd-console-api.png`、数値は実測値）。
- 旧認証はサーバー側で無効のため、更新後は再ログインと再保存が1回必要になる。旧記録は本体起動時に破棄する。
- 全フラッシュ退避は `.private/backups/before-console-api-20260922d.bin` に保存し、`verify-flash` のdigest一致を確認した。退避時は `--after no_reset` で読み出し後にリセットさせず、そのまま `--before no_reset` で照合した。Wi-Fi接続中の再起動はDHCP取得IPなどをNVSへ書き戻すため、リセットを挟むとdigestが合わなくなる。

## v0.2.2リリース（2026-09-16）

`package.json` を0.2.2に上げ、`Unreleased` を `## [0.2.2] - 2026-09-16` に移した（重複していたFixed節を統合）。公開前にホスト検査40件・native 36件・ESP32ビルド・CA照合・`bun audit`（脆弱性なし）・mypy strict・ruff整形が成功し、mainのCI成功後に未使用の `v0.2.2` タグを公開した。検証記録の追加コミット後のCIはDPAPI制限時間とUSB worker起動の3件で失敗したが、同一コードの再実行で成功し、 runner側の一時的な失敗と確認した。Releaseワークフローは成功し、公開ノートがCHANGELOGの0.2.2節と一致することを確認した。4成果物を展開し、ホストZIPの `start.cmd`・USB worker・`lib/sensitive-xpt2046`・版数0.2.2、ファームウェアZIPの個別イメージと `firmware-full.bin`（0x10000のアプリ領域一致・ESP32 magic確認）・同梱ドライバー、秘密情報やキャッシュを含まないことを確認した。

## 8-bit Sprite全幅消去の修正（2026-09-14、実機未検証）

povo専用基板で見つかったタブ切替時の画面残像と同じ、TFT_eSPI基底参照経由の`fillScreen`を使用していた。v0.2.1では320×240全体を`fillRect`で消去するよう修正し、右端まで消去するホスト回帰テストを追加した。native 33テストとPlatformIOビルドが成功した。この版の表示・タッチ実機検証は未実施。

確認日: 2026-09-09

## 描画・タッチ方式移行（2026-09-14、実機未検証）

通常画面をTFT_eSPIの8-bit Sprite描画、タッチ入力を通知用CYDのXPT2046ドライバーへ移行した。ファームウェアビルドとnative 32テストに成功した。差分転送は初回の全帯、同一画面の省略、単一帯の更新、連続帯の結合をテストした。従来の実機検証結果は移行前の記録である。今回のWindows昇格ビルドでは同梱esptoolのintelhexファイルに昇格側アカウントの読み取り権がなく、一時的に同じ依存を作業領域へコピーして`PYTHONPATH`から参照した。製品ファイルやパッケージの権限は変更していない。

v0.2.0のmain CIとタグ起点リリースは成功。公開されたホスト・ファームウェア両ZIPのSHA-256を照合し、両アーカイブに`lib/sensitive-xpt2046`と差分描画ヘッダが含まれることを確認した。実機の表示・タッチ経路は未検証。

専用基板で、全タブの文字と数値、タブ・自動消灯スライダー（分・時間・取得間隔）とスクロールバー、通常・反転のタッチ、消灯後の復帰、起動後のBOOT短押しによる反転、長押しによる3点のタッチ位置・軸方向・押圧感度調整、再起動後の保持、誤タップなしを確認する。差分転送で表示欠け・ちらつきがないこと、TLS通信中にSprite用メモリが不足しないこと、USBの`screenshot`で実画面を読み戻せることも確認する。リリース時はホスト・ファームウェア両アーカイブに`lib/sensitive-xpt2046`が含まれることを展開して確認する。接続中のCodex Micro基板はこの検証に使用しない。

## 実機で確認したこと

対象はESP32-2432S028R、ILI9341、4MBフラッシュ。接続ポートはこの環境ではCOM3でした。

| 項目                   | 結果                                                                                         |
| ---------------------- | -------------------------------------------------------------------------------------------- |
| 元の全フラッシュの退避 | 4,194,304バイトを保存し、実機とのdigest一致を確認                                            |
| ログインの再利用       | 指定されたログイン済みFirefoxから認証を取得し、DPAPI保存後に別プロセスで復号・公式取得に成功 |
| 初期設定               | 利用者が入力したWi-Fi設定と認証をUSBで保存し、相関ID付きACKを確認                            |
| 直接HTTPS              | ESP32がNTP同期後、正しいCA束で公式OpenCodeへ接続して3期間を取得                              |
| PC不要の動作           | PC設定アプリを停止した状態で、ESP32をリセットし、60秒差の2回の `source:http` 使用量ACKを受信 |
| 設定保持               | 再起動後、SSID・認証の再入力なしでWi-Fi接続と公式取得に成功                                  |
| LCD                    | 実際のILI9341 GRAMを読み出し、3期間の割合・金額・バー・リセット時間を画像確認                |
| 不正入力               | 欠損、0の上限、長すぎる行を拒否し、その後の正常な行を受信できることを確認                    |
| 元の状態への復元       | 検証後、元の全4MBを書き戻し、`verify-flash` の完全一致を確認                                 |

LCD表示の公開例は [lcd-sample.png](docs/lcd-sample.png) です。公開画像とテストの数値は検証用の架空値で、認証情報やWi-Fi情報を含めていません。

元ファームウェアのSHA-256:

```text
023E3F264C912109865DF92FA2949571F854692124C0014A58F2848F82A3D132
```

このハッシュは今回退避した機器固有の検証記録です。別の機器へ固定して使いません。

## 自動検証

以下の実行を確認しました。

```powershell
bun run lint
bun run format:check
bun run type-check
bun run build
bun run test
bun run audit
python -m mypy --strict host/serial-worker.py scripts/update-ca.py scripts/capture-lcd.py
python -m ruff format --check host/serial-worker.py scripts/update-ca.py scripts/capture-lcd.py firmware/test/native-toolchain.py
python scripts/update-ca.py --check
$env:PYTHONUTF8 = '1'
pio test -e native
pio run -e esp32dev
```

- ホスト: 40件のテスト成功。公式取得、型と金額換算、認証保存、USBプロセス終了、設定ACK相関、誤った機器への認証送信防止、ローカル操作の保護を確認。
- C++: 21件のテスト成功。3期間の解析、重複・不整合拒否、取得先ID抽出、バー境界値、古さの判定を確認。
- Python: strict型検査と整形確認成功。
- ESP32ビルド: RAM 72,760 / 327,680バイト（22.2%）、アプリ領域969,781 / 1,310,720バイト（74.0%）。
- npm依存: `bun audit` は98パッケージに既知の脆弱性なし。pySerial 3.5はOSV照会で該当報告なし。
- CA: Google公式のGTS R4とGlobalSign Root CA（gsr1）のPEMに完全一致し、SSL証明書として解析できることを確認。

実行環境はWindows x64、Bun 1.4.0、Python 3.14、Node.js 26.4.0（テストfixture用）、PlatformIO Core 6.1.19でした。ネイティブC++テストにはインストール済みLLVMを使用しました。製品のUSB通信にNode.jsは使いません。

## 配布物

リリースワークフローのアーカイブ組み立てをローカルでも実行し、WindowsホストZIPとファームウェアZIPを生成しました。初期設定ランチャー、Python worker、ファームウェア全体イメージ、ソース、ライセンスを照合し、認証ファイルやキャッシュを含まないことを確認しました。展開したホストからUSBポートの列挙とヘルプ表示も成功しています。

## 操作画面

Firefoxで表示・使用量更新・言語切り替えと保持・USBポート再検索・入力検証を確認しました。ChromeではCSS表示幅320、390、768、3840ピクセルを実測し、横方向のはみ出しやJavaScript実行エラーがないことを確認しました。

保存ボタンはログイン済み・USB接続済み・対応ファームウェア確認済みのときだけ有効です。別のファームウェアへCookieを送らないことを自動テストで確認しています。

## 発見した問題と対策

- CH340とserialportネイティブbindingの書き込み停止を、Bun・Node.js両方で再現。実機で安定したpySerialへ変更した。
- 子プロセス終了後の書き込みによるEPIPEを修正。終了時はEOFを渡し、タイムアウトでプロセスを終了してUSBポートを解放する。
- Preferencesの空文字保存が0を返す問題を、スキーマ番号とCRC付きの一括保存へ変更して解消した。
- CAの転記不備と信頼するルートの取り違えを修正。公式PEMから生成するスクリプトと照合を追加した。
- 64KiBの固定バッファによるDRAM不足を、HTML/JSの逐次走査に変更して解消した。

## 未実施の範囲

- mainのGitHub Actionsを実行し、Windows改行差と初回DPAPI処理のテスト期限を修正しました。リリース成果物は同じワークフローで生成します。公開結果はGitHubのReleasesを参照してください。
- GitHub／Googleへの新規ログイン完了は既存セッションで代替しました。新しい専用Firefoxが公式の認証選択画面へ到達することは確認済みです。
- 実際の公式デプロイによる取得先ID失効、期限切れCookie、CA更新、長期間の連続稼働は実機では再現していません。関連する形式検査・失敗表示は実装し、再検証時も0%として誤表示しないことを確認します。
- スマートフォンだけでの初期設定には対応していません。今回の初期設定はWindows PCを使います。

## 再検証の順序

1. 全フラッシュを退避・照合してから検証用ファームウェアを書き込む。
2. PCでログインしてUSB接続し、Wi-Fi設定を保存してACKを確認する。
3. LCDの `OpenCode updated` または操作画面の直接更新確認を確認する。
4. PC設定アプリを停止し、ESP32をリセットする。
5. Wi-Fi、NTP、HTTPS取得が再成立し、複数回の更新が続くことを確認する。
6. 必要な画面検証を終えてUSBを解放し、元の全フラッシュを復元・照合する。

LCDの画像を再取得する場合は、設定アプリのUSB接続を切断した上で、LCD用ファームウェアが動く機器へ実行します。

```powershell
python scripts/capture-lcd.py --port COM3 --output .private/lcd.png
```

## 自動消灯・LCD内設定タブ（0.1.0）

- 実機で9値すべての保存ACKを確認。15秒設定は15.016秒、30秒設定の再起動後は29.985秒でGPIO21のLOWを確認。
- 消灯後の使用量フレームで再点灯しないこと、長い画面メモリ読出し中も独立タイマーで消灯することを確認。
- 実機のタッチ座標から縦横を入れ替える変換へ補正し、利用者によるDisplayタブ→1m選択、Usage/Display往復、消灯後のタッチ起床と選択維持を確認。
- 同じ長押しを複数の設定操作にしないよう、離した状態が20ms続くまでのラッチをnativeテストで確認。
- schema 2から3へのCRC付き移行で認証/Wi-Fi全フィールドを維持すること、Wi-Fi無効時にも消灯時間を復元することをnativeテストで確認。
- PC設定画面の9値すべての選択・保存、点灯操作、狭い幅での表示を実ブラウザで確認。

[タブ操作の確認画像](docs/lcd-display-settings.png) の最下部は、検証用データの更新を止めた際の表示です。

## BOOTによる画面反転（0.1.1、2026-09-10）

今回も接続機器を列挙し、COM3のESP32-D0WD-V3・4MBフラッシュを確認しました。書き換え前に全4,194,304バイトを別名で退避し、実機とのdigest一致を確認しました。

- GPIO0への押下・解放入力で反転の保存ACKを確認し、リセット後にも向きが維持されることを確認。
- 1.5秒間の長押しでは反転せず、離したときに1度だけ反転することを確認。
- 15秒での消灯後、BOOTクリックにより点灯・反転することを確認。消灯時間の変更後も向きを保持。
- 約20秒かかるLCDメモリ読出しの最中にクリックしても、転送完了後に1度だけ反転することを確認。通信とは独立したボタン検出を実機で検証。
- 利用者の物理BOOT操作で通常／反転の両状態を確認。両方の向きでDisplay→1m→Usageを実際にタッチし、raw座標・変換座標・操作名・保存ACKを照合。
- 反転時のDisplayタップはrawX=3361、rawY=1378からx=225、y=33へ、1mはrawX=2755、rawY=880からx=270、y=74へ変換された。これは今回の基板での観測値で、判定をこの座標へ固定していない。
- [実機から読み出した反転表示](docs/lcd-boot-flipped.png) を確認。数値は架空の検証値で、下端のSTALE表示は更新を止めたため。

今回の検証開始時はWi-Fi無効の保存状態でした。反転操作による既存設定の保持は実機で、認証を含むschema 2/3の移行・保存失敗時の維持はnativeテストで確認しています。今回の追加検証では新しい公式HTTPS取得や新規ログインを実行していません。直接取得の実機結果は上記2026-09-09の記録を参照してください。

## ちらつきの修正と0.1.1の自動検証

- 修正前後の実機へ、表示値を固定し取得時刻だけを変えた使用量を12回送信。ACK受信までの中央値は202.023msから56.061msへ短縮した。これはUSB通信時間を含み、画面のリフレッシュレートを意味しない。
- 同一サンプルを実機GRAMから読み出したRGB888画像を比較し、修正前後の全230,400バイトが一致した。表示内容・文字位置・色を維持していることを確認。
- LCDへ消去画面を転送せず、10KiBの作業領域で完成した16行の帯を転送する。追加の全面フレームバッファや描画ごとのメモリ確保はない。
- 最終ファームウェアのビルドはRAM 83,184 / 327,680バイト、アプリ972,749 / 1,310,720バイト。Wi-Fi無効の比較試験ではfreeHeap 249,372バイト、minFreeHeap 243,644バイトだった。
- ホスト40件、native C++34件のテスト成功。帯の境界、デバウンス・長押し・時刻桁あふれ、反転後の両タブと9ボタン、旧schemaの移行、NVS保存失敗後の旧設定保持を含む。
- lint、整形、TypeScript/Python型検査、ホストとESP32ビルド、CA公式ファイル照合、`bun audit`（98パッケージに既知の脆弱性なし）を確認。
- 描画変更後も実機のBOOT反転・再起動後の保持・長押し・消灯後の点灯・USB画像転送中のクリック保持を再実行して成功した。ちらつきについては消去転送を除いた実装と前後の実機画像一致を確認したが、利用者による修正後の見た目の再確認は未取得。
- 空の出力ディレクトリへリリースワークフローと同じ処理でZIP/tar.gzを組み立て、ファームウェア・同梱ソース・結合イメージ内のアプリ位置を照合した。ホストのランチャー・USB worker・バージョン・更新手順・ライセンスが入り、秘密情報やキャッシュが含まれないことを確認。配布ホストからヘルプとポート一覧も表示できた。
- mainのGitHub Actionsでホスト検査・ESP32ビルド・nativeテストが成功した。
- 最後に検証前の全4,194,304バイトを同じ実機へ書き戻し、`verify-flash`でdigestの完全一致を確認した。実機に残した状態は検証前のファームウェア・設定である。

## COM6基板への現行ファームウェア書き込み（2026-09-16）

対象はCOM6のESP32-D0WD-V3・4MBフラッシュ（`flash-id`で実測）。COM3/COM4は不在だった。書き換え前に全4,194,304バイトを機器別の退避先へ保存し、`verify-flash`（115200）でdigest一致を確認した。退避バックアップは `.private/` に保管し、既存の `original-flash.bin` は上書きしていない。

- `pio run -e esp32dev` が成功（RAM 73,516 / 327,680バイト、アプリ991,669 / 1,310,720バイト）。
- `pio run -e esp32dev -t upload --upload-port COM6` は自動リセットで `Wrong boot mode detected (0x13)` となり、BOOT押下保持中の再実行で23秒で成功した。NVS保持のupload方式のため `firmware-full.bin` のオフセット0書き込みは使っていない。
- 書き込み直後はBOOT保持のままリセットされて書込待機になっていたため、BOOTを離してRSTを押して起動させた。
- `{"version":1,"type":"ping"}` に対し `firmware=opencode-go-lcd`、`setupSchema=3` のACKを確認した（`version`なしのpingは `unsupported protocol version` になる）。応答時は `wifiEnabled=false`、`hasUsage=false`、`backlightTimeoutSec=60` だった。
- 書き込み確認中にタッチの `tap` イベント受信も確認した。表示・タッチの実機目視検証と、退避バックアップの書き戻しは未実施（書き込み自体が依頼のため現行ファームウェアを残している）。

## COM6基板の初期設定（2026-09-16）

書き込み直後のCOM6基板に対し、PC操作画面（`start.cmd`→`http://127.0.0.1:8765`）から初期設定を行った。事前に `ping` で `firmware=opencode-go-lcd`・`setupSchema=3` を確認してから認証を送った。

- 操作画面でログインを完了し、COM6へ接続、SSID・パスワード・取得間隔を保存して保存完了応答を確認した。
- `/api/status` で `authenticated=true`、COM6の `connected=true`・`compatible=true`、「ESP32の直接更新を確認」（`lastDirectUpdate` あり）、使用量の取得を確認した。
- 保存後の直接 `ping` 再確認は、操作画面がCOM6を保持しているため行わず、状態は上記の `/api/status` で確認した。
- PC停止後の再起動保持や60秒間隔の継続更新の再確認は未実施。行う場合は操作画面を停止しESP32をリセットして複数回の更新を確認する。

## COM6基板のタッチずれ診断と3点校正化（2026-09-16、実機検証済み）

2点校正後も位置の大ズレと反転感が残るとの報告を受け、USBの `type:"touch"` 診断を90秒取得した（操作画面は切断しCOM6を確保）。成功タップ4件は左側rawX約540..630・右側約3430..3520で、rawYはどちらも約450..630だった。非入替配線（rawX→画面X・rawY→画面Y）のため、入替固定の現行式では右側タップが左端に写る。2点対角は入替と非入替で同じ測定になるため校正では直らない構造だった。残り約410件は `unreadable` で、押圧閾値が通常タップを弾いていた。

- BOOT長押しの校正を3点（左上・右上・左下）にし、同一行・同一列の測定差から軸方向を自動判定する。NVS `opencode-touch` はschema v2（旧v1は入替として移行、設定は保持）。
- 校正画面の表示を「いつもの強さで押す」旨に改め、3点の最弱押圧から閾値を決める。`ping` 応答に `touchCalibrated`/`touchThreshold` を追加した（ホストの互換判定は無影響）。
- native 36件（非入替・入替・反転・ spans不足・旧記録移行を含む）とESP32ビルドが成功した。
- COM6へ書き込み、旧v1記録の移行（`touchCalibrated=true`・旧閾値120・Wi-Fi設定保持）を確認した。3点再校正後は閾値12となり、四隅・中央タップが正しい領域に写ることを確認した（例：DisplayタブrawX約2380..2630→x約219..244・y約10..13、右下rawX約3470→x=319・y約222）。

## PENIRQノイズの無接触イベントと起床・遅延への影響対策（2026-09-16、実機検証済み）

3点校正後の診断で、無接触でも約4件/秒の `unreadable` が出続けることを確認した（20秒無接触で76件）。割り込み毎にバックライト期限を更新していたため自動消灯が成立せず、150msの通信遅延や診断スパムも発生していた。ホスト側はtouch行を無視するため影響なし。

- `pollTouch` を改め、PENIRQがHIGH（指なし）のグリッチは無音で捨て、読み取り成功後のみバックライトを更新する。消灯中の実接触は従来どおり起床する。
- 無接触30秒で0件、30秒放置で `backlightRemainingMs` が約31秒減少（期限更新なし）、直接HTTPS取得の成立（`hasUsage=false`→`true`）を確認した。
- Display/Usageタブの交互タップ34件が全て正しく判定され、`others` は0件だった。

## Dependabot 自動処理（2026-09-23）

`.github/workflows/dependabot-automation.yml` を actionlint で検査し、PR 用 workflow 名（CI）と一致することを確認する。Dependabot の patch／minor かつ全 PR チェック成功の場合だけ取り込み、major・古い SHA・再失敗は残す。

実際の Dependabot PR がまだない場合、動作経路は未検証として扱う。実 PR 発生後に自動化ジョブ、CI の再試行、マージ結果を確認する。

大量の Dependabot PR により CI 完了より分類が遅れる場合でも、分類後の `workflow_dispatch` が現在の PR 番号と head SHA を照合して再評価する。別の作成者、古い SHA、未完了の CI はマージしない。
